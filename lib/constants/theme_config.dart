import 'package:flutter/material.dart';

/// 勿动！  主题蓝色
const Color primaryColorBlue = Color(0xFF355EBF);

/// 勿动！  主题玫粉色
const Color primaryColorPink = Color(0xFFFA338A);

/// 勿动！  客服电话
const int customerPhoneNumber = 669082;

const double pageHorizontalPadding = 12; // 组件的水平内边距

const double appbarHeight = 46; // appbar的高度

const Color pageShadowColor = Color.fromRGBO(0, 0, 0, 0.1); // 卡片的boxShadow颜色
const Color lightBlue = Color(0xFFD9E4F0);
const Color mistyPink = Color.fromARGB(255, 255, 248, 251);
const Color lightPink =  Color.fromARGB(255, 255, 215, 246);
  // 深蓝色 透明
const Color deepBlueTransparent = Color.fromARGB(141, 6, 33, 122);

// 冰蓝色 色系 深浅
const Color accentTealColor = Color(0xFF2481A0);
const Color accentTealDeepColor = Color.fromARGB(255, 1, 83, 110);
// 紫色系 深
const Color accentPurpleDeepColor = Color.fromARGB(255, 147, 0, 167);

/// 时间轴节点紫色
const Color timelinePurpleColor = Color.fromARGB(255, 134, 134, 134);

///  酒红色
const Color wineRedColor = Color.fromARGB(255, 180, 7, 7);
///  绿色  成功
const Color greenColor = Color.fromARGB(255, 0, 180, 7);


const double middleScreenWidth = 480; // 中屏幕宽度
const double largeScreenWidth = 534; // 533以上为大屏幕宽度

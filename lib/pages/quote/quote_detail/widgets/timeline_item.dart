class TimelineItem {
  final int id;
  final DateTime createdAt;
  final int customerId;

  TimelineItem({
    required this.id,
    required this.createdAt,
    required this.customerId,
  });
}
